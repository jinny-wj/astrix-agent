export default function HeroSection() {
  return (
    <section className="flex flex-col items-center pt-[calc(var(--space-4xl)+var(--space-2xl)+var(--space-xs))] text-center">
      <h1 className="text-[22px] font-semibold leading-[32px] tracking-[-0.025em] text-[#0f1419] sm:text-[24px] sm:leading-[36px]">
        把设计想法，快速变成
        <span className="ml-space-sm text-[var(--home-accent)]">
          可交付物料
        </span>
      </h1>
    </section>
  )
}
